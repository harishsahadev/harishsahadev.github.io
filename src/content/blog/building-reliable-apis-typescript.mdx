---
title: "Building Reliable APIs with TypeScript"
description: "How strict typing at the boundary layer eliminates a whole class of runtime bugs — and why I'll never go back to untyped fetch calls."
pubDate: 2025-01-20
tags: ["typescript", "api", "backend"]
draft: false
---

There's a specific kind of bug that only appears in production, at 2am, when the third-party API you depend on quietly changes the shape of its response. You've seen it. I've seen it. TypeScript alone doesn't save you — but typed API clients do.

## The problem with raw fetch

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.

```ts
// The naive approach — anything could be in here
const res = await fetch("/api/users");
const data = await res.json(); // data: any 😬
```

Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident.

## Zod at the boundary

Sunt in culpa qui officia deserunt mollit anim id est laborum. Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

```ts
import { z } from "zod";

const UserSchema = z.object({
  id: z.string().uuid(),
  name: z.string(),
  email: z.string().email(),
  createdAt: z.coerce.date(),
});

type User = z.infer<typeof UserSchema>;

async function getUser(id: string): Promise<User> {
  const res = await fetch(`/api/users/${id}`);
  const raw = await res.json();
  return UserSchema.parse(raw); // throws if shape is wrong
}
```

Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.

## What I learned

Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum. This approach catches breaking changes from third-party APIs immediately, in your test suite, before they ever reach users.

> The best error message is the one that fires at parse time, not at render time when a user is watching.

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
