---
interface Props {
  title: string;
  description: string;
  pubDate: Date;
  tags: string[];
  slug: string;
  /** Estimated reading time in minutes — calculate externally or use a remark plugin */
  readingTime?: number;
}

const { title, description, pubDate, tags, slug, readingTime } = Astro.props;

const formattedDate = pubDate.toLocaleDateString("en-US", {
  day: "numeric",
  month: "short",
  year: "numeric",
});

const iso = pubDate.toISOString();
---

<a href={`/blog/${slug}`} class="blog-card" aria-label={title}>
  <article>
    <header class="blog-card-header">
      <div class="blog-card-meta">
        <time datetime={iso} class="blog-date">{formattedDate}</time>
        {readingTime && (
          <>
            <span class="meta-sep" aria-hidden="true">·</span>
            <span class="blog-reading-time">{readingTime} min read</span>
          </>
        )}
      </div>

      <!-- Arrow that animates on hover -->
      <svg class="card-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M5 12h14M12 5l7 7-7 7"/>
      </svg>
    </header>

    <h2 class="blog-card-title">{title}</h2>
    <p class="blog-card-desc">{description}</p>

    {tags.length > 0 && (
      <ul class="blog-tags" role="list" aria-label="Tags">
        {tags.slice(0, 4).map((tag) => (
          <li class="blog-tag">#{tag}</li>
        ))}
      </ul>
    )}
  </article>
</a>

<style>
  .blog-card {
    display: block;
    padding: var(--space-6);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    background: var(--bg-card);
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.06);
    transition: background var(--duration), border-color var(--duration),
                box-shadow var(--duration), transform 250ms var(--ease-out);
  }
  .blog-card:nth-of-type(even) {
    background: color-mix(in srgb, var(--bg-raised) 88%, var(--bg-card));
  }
  .blog-card:hover {
    background: color-mix(in srgb, var(--accent) 7%, var(--bg-card));
    border-color: color-mix(in srgb, var(--accent) 35%, var(--border));
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.1);
    transform: translateY(-3px);
  }

  .blog-card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-3);
  }

  .blog-card-meta {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-faint);
  }
  .meta-sep { user-select: none; }

  .card-arrow {
    width: 20px;
    height: 20px;
    color: var(--text-faint);
    transform: translateX(0) scale(1);
    transition: transform 200ms var(--ease-out), color 200ms;
  }
  .blog-card:hover .card-arrow {
    transform: translateX(6px) scale(1.1);
    color: var(--accent);
  }

  .blog-card-title {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    line-height: 1.3;
    color: var(--text);
    margin-bottom: var(--space-2);
    transition: color 150ms;
  }
  .blog-card:hover .blog-card-title { color: var(--accent); }

  .blog-card-desc {
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: var(--space-4);
    /* Clamp to 2 lines */
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .blog-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    list-style: none;
  }
  .blog-tag {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    color: var(--text-faint);
    letter-spacing: 0.02em;
    transition: color 150ms;
  }
  .blog-card:hover .blog-tag { color: var(--text-muted); }

  @media (max-width: 640px) {
    .blog-card {
      padding: var(--space-4);
      border-radius: var(--radius-md);
      box-shadow: none;
    }

    .blog-card-header {
      align-items: flex-start;
      gap: var(--space-3);
      margin-bottom: var(--space-4);
    }

    .blog-card-meta {
      min-width: 0;
      font-size: 0.6875rem;
    }

    .card-arrow {
      width: 18px;
      height: 18px;
      flex-shrink: 0;
      margin-top: 0.1rem;
    }

    .blog-card-title {
      font-size: 1.05rem;
      line-height: 1.25;
      margin-bottom: var(--space-3);
    }

    .blog-card-desc {
      font-size: 0.8125rem;
      line-height: 1.65;
      margin-bottom: var(--space-3);
    }

    .blog-tags {
      gap: 0.35rem 0.5rem;
    }

    .blog-tag {
      font-size: 0.625rem;
    }
  }
</style>
